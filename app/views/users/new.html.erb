<h2>スケムール新規作成</h2>

<%= form_for @user do |f| %>
    <table>
        <tr>
            <th><%= f.label :title , "タイトル" %></th>
            <td><%= f.text_field :name %></td>
        </tr>
        <tr>
            <th><%= f.label :start_day , "開始日" %></th>
            <td><%= f.date_field :start_day %></td>
        </tr>
    </table>
<%= link_to "スケジュール一覧に戻る" :users %>